{
  "name": "fido",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "react-app-rewired build",
    "start": "PUBLIC_PATH=http://localhost:3000/ react-app-rewired start",
    "bdd": "PUBLIC_PATH=http://host.docker.internal:3000/ react-app-rewired start"
  },
  "dependencies": {
    "react-scripts": "^1.0.17"
  }
}
